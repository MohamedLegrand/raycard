services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: raycard
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "123"
    ports:
      - "5432:5432"
    volumes:
      - raycard_pg_data:/var/lib/postgresql/data

volumes:
  raycard_pg_data:
